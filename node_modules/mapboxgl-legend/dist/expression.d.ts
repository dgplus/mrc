import type { Expression, ExpressionName } from 'mapbox-gl';
export declare type ParsedExpression<In, Out> = {
    name: ExpressionName;
    stops: [In, Out][];
    inputs: In[];
    outputs: Out[];
    min: number;
    max: number;
};
declare const _default: {
    isExpression: (e: any) => e is Expression;
    parse: (input: any) => ParsedExpression<any, any> | null;
};
export default _default;
