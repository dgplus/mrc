import { IControl } from 'mapbox-gl';
import type { Map } from 'mapbox-gl';
export declare type LayersView = string[] | Record<string, boolean | string[]>;
export declare type LegendControlOptions = {
    collapsed?: boolean;
    toggler?: boolean;
    layers?: LayersView;
    onToggle?: (layer: string, visibility: boolean) => void;
};
export default class LegendControl implements IControl {
    private _options;
    private _container;
    private _map;
    constructor(options?: LegendControlOptions);
    onAdd(map: Map): HTMLElement;
    onRemove(): void;
    addLayers(layers: LayersView): void;
    removeLayers(layerIds: string[]): void;
    private _isAttributeVisible;
    private _getBlock;
    private _toggleButton;
    private _loadPanes;
}
